{
  "extends": "next/core-web-vitals",
  "rules": {
    // Allow unescaped entities in JSX (for natural text like "don't", "you're", etc.)
    "react/no-unescaped-entities": "off",
    
    // Make React hook dependency warnings instead of errors
    "react-hooks/exhaustive-deps": "warn"
  }
}
